@using PokerClubsApp.Data.Models
@using PokerClubsApp.Web.ViewModels.Unions

@model Union;


@{
    ViewBag.Title = "Union details";
}


<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="row">
    <div class="col-sm-12 offset-lg-1 col-lg-10 offset-xl-2 col-xl-8" style="overflow: auto">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Union name</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><p>@Model.Name</p></td>
                </tr>
                <tr>
                    <td>
                        <a asp-controller="Unions" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary d-flex justify-content-center align-items-center flex-fill mx-1">Edit</a>
                    </td>
                   @*  <td>
                        <a asp-controller="GameResults" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger d-flex justify-content-center align-items-center flex-fill mx-1" onclick="return confirm('Are you sure you want to delete this result?');">Delete</a>
                    </td> *@
                </tr>
            </tbody>
        </table>
    </div>
</div>